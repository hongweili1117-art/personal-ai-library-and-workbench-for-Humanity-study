# ============================================================
#  个人 AI 智库 · 依赖清单
#  安装：pip install -r requirements.txt
#  （一键安装脚本会自动完成这一步，普通用户无需手动操作）
# ============================================================

# ---- 必需：缺少其一程序无法启动 ----
Flask>=3.0
PyMuPDF>=1.24        # PDF 渲染 / 取文字，导入名为 fitz
numpy>=1.26          # 语义检索 / 笔记向量
waitress>=3.0        # 本地服务器（无黑框启动器用它在后台稳定运行，支持流式/并发）

# ---- 强烈建议：桌面窗口（无黑框体验最佳） ----
# 装了它，启动时会弹出一个独立的程序窗口（最像 App，关闭窗口即退出）；
# 不装也能用——会自动改用系统浏览器，并附带一个一键退出的小窗口，同样没有黑框。
pywebview>=5.0

# ---- 强烈建议：实际调用 AI 时需要 ----
# OpenAI / DeepSeek / Z.ai GLM / 通义千问 Qwen / Kimi / 自定义 OpenAI 兼容端点 都走这个 SDK
openai>=1.30
# 使用 Claude 作为引擎时需要
anthropic>=0.34

# ---- 可选：装了体验更好，不装也能正常用 ----
# 语义检索（更强的概念匹配）。不装则自动退回本地轻量向量，功能不缺：
# chromadb>=0.5
# fastembed>=0.3
# 把分析报告导出成 Word(.docx)。不装则仍可导出 Markdown：
# python-docx>=1.1
# 整本「扫描版 PDF」转「可搜索 PDF」的命令行工具（见 把扫描版PDF转成可搜索PDF.py）。
# 需要再额外安装系统级的 Tesseract + Ghostscript（详见该脚本顶部说明）：
# ocrmypdf>=16
#
# 进阶（可选系统工具，非 pip 包）：
#   pandoc   —— 导出 Word 时若安装可获得"真脚注 + 卡片表格"
